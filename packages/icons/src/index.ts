// @fxp/icons — placeholder
// Le re-export sélectif de lucide-react (ou icônes custom) viendra dans une
// feature dédiée (architecture/icons-package). Cette version garantit juste
// que le package est résolvable dans le workspace.

export const __PLACEHOLDER__ = true
