---
name: aic-feature-audit
description: Audite un scope ou une fiche feature. Deux modes — `discover <scope>` (rétro-doc du code orphelin) et `refresh <scope>/<id>` (re-sync frontmatter vs code réel). Dry-run par défaut.
---

Suivre `workflow.md` dans ce dossier.
